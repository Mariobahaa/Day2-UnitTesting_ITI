// calculate square root - input: 16 => output: 4
export const squareNumber = (number) => Math.sqrt(number);

//optional
//match on string
// export const squareNumberStatement = (number, unit) => {
//   const calculatedNumber = Math.sqrt(number);
//   return `the result should be ${calculatedNumber} ${unit}`;
// };
